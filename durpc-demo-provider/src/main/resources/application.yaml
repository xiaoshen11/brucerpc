server:
  port: 8081
durpc:
  zkServer: localhost:2181
  zkRoot: durpc

app:
  id: app1
  namespace: public
  env: dev
  metas: "{dc: 'bj', gray: 'false', unit: 'B001'}"

logging:
  level:
    root: error
    com.bruce.durpc: info